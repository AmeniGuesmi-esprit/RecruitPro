package com.recruitment.subscription.model;

/** Type d'abonnement : COMPANY (quota = nombre d'offres publiables) ou CANDIDATE (quota = nombre de candidatures). */
public enum SubscriptionType {
    COMPANY,
    CANDIDATE
}
